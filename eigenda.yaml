network: holesky

ethRpcUrl: "https://ethereum-holesky-rpc.publicnode.com"

persistence:
  size: 150Gi

service:
  loadBalancerIP: "121.138.74.21"

node:
  resources:
    limits:
      cpu: 800m
      memory: 1Gi
    requests:
      cpu: 800m
      memory: 1Gi

secrets:
  blsKeystore: "dummy-bls-keystore"
  blsKeystorePassword: "dummy-bls-password"
  ecdsaKeystore: "dummy-ecdsa-keystore"
  ecdsaKeystorePassword: "dummy-ecdsa-password"