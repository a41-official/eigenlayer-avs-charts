apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lagrange-state-committees.fullname" . }}-test-connection"
  labels:
    {{- include "lagrange-state-committees.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lagrange-state-committees.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
